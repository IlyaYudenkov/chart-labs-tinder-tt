export enum EButtonType {
    SUBMIT = 'submit',
    RESET = 'reset',
    BUTTON = 'button',
}

export enum EButtonVariants {
    default = '',
    gradient = 'bg-gradient-red-to-orange rounded-[22px] p-2 font-bold text-primary active:scale-98 active:brightness-95 transition-transform transition-filter duration-200 ease-in-out',
}
